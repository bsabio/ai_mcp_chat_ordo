# Phase Status Board

Date: 2026-04-12

## Current Snapshot

| Phase | Status | Loop state | Current blocker or finding | Next evidence |
| --- | --- | --- | --- | --- |
| [Phase 0](./phase-0-guardrails-and-parity-gates.md) | Complete | Exit criteria verified | Provider parity, prompt-surface parity, deferred-job capability coverage, and worker startup validation are now frozen by tests | Keep the Phase 0 verification bundle green as later phases land |
| [Phase 1](./phase-1-chat-pipeline-decomposition.md) | Complete | Exit criteria verified | Route orchestration now lives in `stream-route-handler.ts`, and `ChatStreamPipeline` delegates to explicit intake, preparation, short-circuit, and execution stage modules | Keep the five-file stream verification bundle green while later phases change provider or catalog seams |
| [Phase 2](./phase-2-provider-runtime-unification.md) | Complete | Exit criteria verified | `provider-runtime.ts` now owns the shared attempt-level seam for both chat transports, and `/api/chat` plus stream timeout regressions are covered by the focused Phase 2 bundle | Keep the focused provider-runtime verification bundle green while later phases change catalog or prompt seams |
| [Phase 3](./phase-3-capability-catalog-and-registration-decomposition.md) | Complete | Exit criteria verified | `CAPABILITY_CATALOG` still composes all 56 capabilities from extracted family modules behind the existing public API, `tool-composition-root.ts` still drives bundle sequencing from one ordered registrar table, shared registration tables still cover all 11 bundles, and the runtime surface now binds every capability through catalog-owned schema plus bundle-local dependency wiring | Keep the documented 11-file focused and 13-file broader Phase 3 verification bundles green as later phases change prompt or execution-target seams |
| [Phase 4](./phase-4-prompt-runtime-contracts-and-invalidation.md) | Complete | Exit criteria verified | `prompt-runtime.ts` now owns the explicit prompt input/output seam across `chat_stream`, `direct_turn`, and `live_eval`; governed slot mutations revalidate the admin prompt surfaces, emit `prompt_version_changed` events for affected active conversations, and prompt provenance can replay the effective prompt assembly contract | Keep the documented 22-file Phase 4 confidence bundle green as later phases change event or platform seams |
| [Phase 5](./phase-5-chat-and-job-event-separation.md) | Complete | Exit criteria verified | Assistant lifecycle now converges on `/api/chat/stream` through `generation_*`, while deferred-job state converges through the shared job publication and read-model seam behind `/api/chat/events` and `/api/jobs/events`; audit-only notification events no longer override visible job state | Keep the documented 30-file Phase 5 unit bundle, clean production build, and 3-spec browser proof green as later platform seams change |
| [Phase 6](./phase-6-platform-delivery-boundary-cleanup.md) | Complete | Exit criteria verified | Platform-delivery rules are now explicit: `/api/chat` uses an explicit intake stage, `AppShell` uses one shared document-flow branch, and overdue follow-ups now live under `src/lib/admin/pipeline` with operator compatibility re-exports | Keep the documented Phase 6 verification bundle and targeted overdue ownership assertion rerun green as later phases land |
| [Phase 7](./phase-7-execution-target-abstraction-and-mcp-sidecars.md) | In Progress | Real Docker-backed container parity is proven, native and remote adapters are live, and sidecar inventory is centralized; broader sidecar operations and wider coverage are still pending | Capability definitions already carry runtime, browser, job, and MCP signals, `admin_web_search` now has host-versus-MCP parity through managed reusable sidecars across `host_ts`, `mcp_stdio`, and a real Docker-backed `mcp_container` pilot, planner-driven `native_process` and `remote_service` overrides now execute through the same runtime-binding seam, and sidecar inventory now reuses one canonical metadata map, but broader sidecar supervision plus browser-WASM and non-pilot target-family coverage are still pending | Extend positive, negative, and end-to-end coverage across each active execution-target family, then broaden sidecar operations beyond the pilot and choose the first production native or remote family |
| [Phase 8](./phase-8-core-pack-separation-and-heavy-runtime-externalization.md) | In Progress | Ownership is explicit in code, compatibility-only surfaces are deleted, `compose_media` routes through a dedicated media worker and now prefers a planner-declared local `native_process` target before deferred fallback, the exported admin-intelligence tools including `admin_search` are now sidecar-first by default, the deferred-job registry plus handler assembly and startup contract now derive from catalog-backed job metadata, and the old blog/book/books/corpus route families are gone; the deferred-job cleanup frontier is reduced to intentional custom executors | The first clean greenfield cut is live, and the biggest remaining work has shifted back to broader pack/runtime adoption rather than duplicated deferred-job plumbing | Broaden the first media native-runtime pilot, extend parity coverage around non-host pack runtimes, and choose the next production native or remote workload after `compose_media` |
| [Phase 9](./phase-9-canonicalization-closeout-and-runtime-promotion.md) | In Progress | Conversation media asset discovery is live, audio/browser payload continuity is normalized, the dead compatibility helper is gone, the breadcrumb surface is now real, and the next workload after `compose_media` is explicitly named; remaining work is transcript/export continuity plus broader doc closeout | The repo now has a model-callable `list_conversation_media_assets` surface, normalized governed audio metadata through browser-runtime snapshots, a narrowed RepositoryFactory legacy note, and an explicit `generate_audio` `remote_service` follow-on decision, but the later-turn replay/export continuity work and final doc harmonization are still pending | Finish transcript and export continuity for reusable media assets, then close the remaining Phase 8 and Phase 9 documentation drift |
| [Phase 10](./phase-10-deferred-job-completion-reliability-and-observability.md) | In Progress | The deferred-job audit is now split into active defects versus preserved invariants, and the latest cut closed the three remaining follow-ups: editorial cancellation now propagates into Anthropic and OpenAI provider calls, a worker-enabled Playwright proof now seeds and completes a real queued draft job in-browser, and admin diagnostics now expose the canonical runtime-audit log directory and file paths | Deferred-job truth is now aligned across chat, workspace, runtime integration, and a live browser worker path, but long opaque downstream providers can still only be interrupted if they honor abort signals, and Phase 10 still needs one final decision on completion-surface policy plus any follow-on for browser-owned forensics | Keep the focused 36-test plus worker-enabled Playwright proof green, then decide whether any remaining opaque providers or browser-owned forensic gaps justify another Phase 10 slice |

## Update Rule

Whenever a phase changes status or loop state, update this file in the same patch as the packet.
